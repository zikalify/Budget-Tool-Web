# Budget Tool Web

An offline-first daily budget planner for GitHub Pages. It keeps wallet data in the browser, supports installable PWA mode, and exports the ledger as CSV.

## Development

```sh
npm install
npm run dev
```

The production output is created with `npm run build` in `dist/` and can be deployed to GitHub Pages as a static site.
